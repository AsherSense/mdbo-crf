# 2026-09-20 验证记录

## 页面简化与10例完整录入（当前发布）

- 移除逐字段填写历史、上次保存值/时间、来源页码、服务器位置文字和界面版本标签。数据库审计记录保持完整，未删除患者数据。
- ECOG分级全文与纳入/排除标准直接显示在对应输入项下，无需展开另一个备注。
- 中央随机分组只保留“确认并随机分组”一个操作，保存资料并执行正式分配；页面持续显示随机号、受试者编号、试验/对照组和对应治疗。保留既有安全资格检查与不可重抽机制。
- 围术期NSAIDs、预防性胰管支架及围术期抗生素仅显示在围术期子页。其他访视隐藏这些字段但保存时保留其旧值；已有错误归属的围术期资料可从围术期历史记录中核对。
- 页面只保留顶部一个“下载患者全部资料”按钮，导出该患者已保存的全部资料。
- 10例完整虚构患者通过真实本地浏览器逐项填写，覆盖6个中心、不同年龄/性别/ECOG/肿瘤类型。每例24或25份适用记录，共244份；包括术前、正式随机、两组对应手术、14天及1/3/6/12月随访、3个量表时点、7个用药时点、7/21/28天检验、不良事件、适用RBO和180天结局。
- 10例逐字段比较保存值与输入值；刷新后随机号/组别不变；10次总下载均包含患者及随机信息。浏览器无脚本错误，390px手机页面无横向溢出。
- 12项自动化测试全部通过，包括上述10例完整资料后端复测、原50例流程及边界/并发/锁定回归。完整数据、测试数据库与下载仅保存在本机测试目录，不进入生产随机序列，不包含真实患者资料。
- 这是软件测试结果，不代表穷尽全部临床边界，也不能替代研究机构正式验收。

## 本次修正复测

### 访视整合与随机状态（本次更新）

- 7个按时间排序的主导航；量表纳入术前/3月/6月，用药纳入各访视，7/21/28天常规检验纳入术后早期，14天与30天保留独立表单。
- 保留所有既有数据库模块ID、记录ID、版本和审计；未归属时间点的历史记录不猜测、不删除，可从相应子页选择核对。
- 保存随机化表单不再显示完成勾号；仅正式分配后显示已分组。缺少筛选/基线时点击分组显示缺项并可跳转，未发起有效分配。
- ECOG 0–5定义及TNM癌种特异说明附官方来源；仅做填写核对，不自动推断分期，不修改获批随机方法。
- 10项自动化测试全部通过，包含50例虚构患者全流程和并发/上限/锁定测试，以及每个字段都有独立填写说明的覆盖检查。
- 每个字段下显示对应填写说明、该记录上次保存值和保存时间，并可按字段展开审计历史（时间、操作者、原值→新值）；不把未保存草稿列为正式修改。浏览器验证年龄54→55的历史与未修改性别字段的空历史相互隔离。
- 浏览器通过：实际分组并刷新保持组别；保存随机资料但未分组提示；缺项跳转；基线与3月量表隔离；3月与6月用药隔离；7天与21天检验隔离；两种CSV下载；ECOG/TNM备注展开；390px无横向溢出、无脚本错误。
- 使用本地测试数据库，未向线上库写入任何测试患者或消耗正式随机号。未刷新用户留有草稿的线上页面。

- 纳入/排除标准改成一个资格选项；兼容既有逐条核对数据，明确选择不符合或待核实时不可随机化。
- 页面真实点击“保存并执行随机化”，验证随机号及中文组别在刷新、切换模块后保持显示；保存与分组期间阻止切换患者。
- 分组后按方案固定两组操作；确认按方案完成后，后台校验直径10mm、跨乳头放置及对应夹类型/数量。两组分别测试，篡改固定值和无理由偏离均被拒绝。
- 补充验证：存活与死亡资料冲突、致死与SAE冲突、起止日期倒置、采血时限超出却达到TBIL比例，不再被误判为符合条件。
- 7项自动化测试全部通过，含50例患者全流程；浏览器下载、刷新及390px手机布局检查通过。

## 数据与算法

本地内存数据库中的50例不同虚构患者，覆盖6个中心、P/NP肿瘤层。每例经过建档、筛选/基线/术中资格保存、一次随机化、重复随机请求、全部模块保存、重复事件分开保存、重新读取及审计记录检查。

额外检查：未登录拒绝；住院号重复拒绝；不符合入组条件拒绝随机化；非法选项、非法日期拒绝；旧版本覆盖拒绝；随机化后依据锁定；分组和审计禁止更新/删除；12个并发请求对3例患者分别只分配一次。

算法检查：平衡评分相同50%/50%，偏向规则80%/20%；272例终止时A/B各136例；超过总数拒绝；日历月月底截断；180天日期。

## 浏览器实测

本机真实浏览器：建档→保存年龄→刷新→重新选择患者→确认数据仍在；保存术后14天记录；点击“下载患者全部记录CSV”和“下载本模块CSV”，取得下载文件并校验包含住院号、年龄及实际日期；1440px桌面和390px手机页面无横向溢出，无脚本错误。

以上测试数据均未写入线上服务器。以上验证不等于证明所有临床边界无错，也不替代机构正式验收。线上平台数据库迁移与发布结果另由部署系统确认。
