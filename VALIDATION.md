# 2026-09-20 验证记录

## 数据与算法

本地内存数据库中的50例不同虚构患者，覆盖6个中心、P/NP肿瘤层。每例经过建档、筛选/基线/术中资格保存、一次随机化、重复随机请求、全部模块保存、重复事件分开保存、重新读取及审计记录检查。

额外检查：未登录拒绝；住院号重复拒绝；不符合入组条件拒绝随机化；非法选项、非法日期拒绝；旧版本覆盖拒绝；随机化后依据锁定；分组和审计禁止更新/删除；12个并发请求对3例患者分别只分配一次。

算法检查：平衡评分相同50%/50%，偏向规则80%/20%；272例终止时A/B各136例；超过总数拒绝；日历月月底截断；180天日期。

## 浏览器实测

本机真实浏览器：建档→保存年龄→刷新→重新选择患者→确认数据仍在；保存术后14天记录；点击“下载患者全部记录CSV”和“下载本模块CSV”，取得下载文件并校验包含住院号、年龄及实际日期；1440px桌面和390px手机页面无横向溢出，无脚本错误。

以上测试数据均未写入线上服务器。以上验证不等于证明所有临床边界无错，也不替代机构正式验收。线上平台数据库迁移与发布结果另由部署系统确认。
